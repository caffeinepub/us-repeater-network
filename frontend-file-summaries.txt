{
  "frontend/src/services/repeaterBookApi.ts": "Service module that defines TypeScript interfaces for RepeaterBook API data, helper functions for mapping raw entries to MappedRepeaterBookRepeater objects, and a new `parseRepeaterBookJson` function that parses raw JSON strings returned by the backend canister. All direct browser fetch calls to RepeaterBook.com have been removed.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations; adds back the missing `useRegisterAdmin` mutation hook that wraps `actor.registerAdmin`, alongside all other hooks for repeaters, favorites, user profile, and admin operations.",
  "frontend/src/App.tsx": "Main application component with TanStack Router setup, QueryClient provider, ThemeProvider, and Toaster. Defines all routes (home, directory, submit, admin, favorites, profile) with a shared RootLayout containing Header and Footer.",
  "frontend/src/components/Header.tsx": "Sticky navigation header with logo, desktop and mobile nav links, and Internet Identity login/logout; conditionally shows admin links (Dashboard, Import CSV) only when the authenticated user has admin role via `useIsCallerAdmin`.",
  "frontend/src/components/Footer.tsx": "Updated footer replacing HamInfo/RadioReference data sources with CHIRP compatibility info, updated brand description to reflect the CHIRP radio memory manager focus.",
  "frontend/src/components/RepeaterCard.tsx": "Repeater card component with updated `FavoriteButton` usage — passes `onClick` (not `onToggle`) with correct event handler signature, and uses the current `RepeaterCardProps` interface (`onClick`, `onFavoriteToggle`, `favoritesDisabled`).",
  "frontend/src/components/FavoriteButton.tsx": "Favorite toggle button supporting disabled state with tooltip explanation, used to disable favoriting for HamInfo-sourced entries while keeping it functional for local repeaters.",
  "frontend/src/components/RepeaterDetailModal.tsx": "Detail modal for a single repeater showing all fields including CTCSS/PL tone formatted with Hz units (amber highlight when present, \"None\" when absent), DCS code, mode, location, and optional coverage/notes/autopatch/link fields.",
  "frontend/src/pages/HomePage.tsx": "Landing page with hero section featuring the antenna logo, feature highlights grid, and a CTA for GMRS repeater submission.",
  "frontend/src/pages/DirectoryPage.tsx": "Repeater directory page using `FilterBar`'s correct `filters`/`onFiltersChange` props interface; uses infinite scroll for unfiltered browsing and full-dataset client-side filtering when search/state/mode filters are active.",
  "frontend/src/pages/SubmitPage.tsx": "Form page for submitting new repeaters (primarily GMRS), with authentication gate, all required fields, and info banner explaining RepeaterBook covers ham radio automatically.",
  "frontend/src/pages/AdminPage.tsx": "Admin CHIRP CSV import page wrapped in `AdminPassphraseGate` with login guard; features drag-and-drop file upload, client-side CSV parsing with parse summary, batched canister writes (200 records/batch) with real-time progress indicator (\"Saving batch X of Y...\"), and a completion summary showing saved/skipped counts and per-state breakdown.",
  "frontend/src/pages/FavoritesPage.tsx": "Favorites page that converts `userPrincipal` from `null` to `undefined` (using `?? undefined`) to match the `useGetFavorites(string | undefined)` signature, fixing the TypeScript type error.",
  "frontend/src/pages/ProfilePage.tsx": "User profile page for setting display name, call sign, and bio, with authentication gate.",
  "frontend/src/index.css": "Global styles with OKLCH color system for the dark navy/charcoal ham radio theme, Orbitron display font for headings, Inter for body text, and custom utility classes.",
  "frontend/tailwind.config.js": "Tailwind configuration with custom color tokens (amber, navy, teal), Orbitron/Inter font families, amber glow box shadows, and animation keyframes.",
  "frontend/src/services/hamInfoApi.ts": "New service file that defines TypeScript interfaces for HamInfo API responses, normalizes raw repeater data into a consistent NormalizedRepeater shape, exports fetchHamInfoRepeaters(state?) for browser-side fetch calls, and includes a RadioReference/hamdb fallback on error. Also exports the full US_STATES list of all 50 states.",
  "frontend/src/components/FilterBar.tsx": "Filter bar with search input, state dropdown (all 50 US states with abbreviations), and mode dropdown; normalizes available states from backend to abbreviations for correct matching, and shows result count or infinite-scroll progress text.",
  "frontend/src/utils/csvParser.ts": "Comprehensive CHIRP CSV parser with robust column mapping (Frequency, Duplex, Offset, Tone, rToneFreq, cToneFreq, DtcsCode, Mode, Comment), full 50-state name/abbreviation normalization, CTCSS/PL tone normalization (treating 88.5 Hz as no-tone), city/state extraction from multiple fields, and per-row error tracking with no row count caps.",
  "frontend/src/components/AdminPassphraseGate.tsx": "Passphrase gate component that first validates the UI passphrase locally, then calls `actor.registerAdmin('adminPassphrase')` on the backend to register the authenticated principal as admin (or confirm existing registration), then refetches `isCallerAdmin` to verify backend admin status before rendering children. Shows appropriate loading, error, and access-denied states.",
  "frontend/src/components/ApprovedRepeatersAdmin.tsx": "Displays a list of approved repeaters for admin management; updated to call `deleteRepeater(repeater.id)` directly (bigint only, no passphrase param), and `passphrase` prop is now optional and unused.",
  "frontend/src/components/PendingRepeatersList.tsx": "Displays pending repeater submissions with approve/reject actions; updated to call `approveRepeater({ repeaterId, approve })` without the removed `passphrase` field, and `passphrase` prop is now optional and unused.",
  "frontend/src/pages/AdminDashboardPage.tsx": "Admin dashboard page with login guard and `AdminPassphraseGate`; contains the full inline `AdminDashboard` component (tabs for pending/approved repeaters with stats) instead of importing a missing external component."
}